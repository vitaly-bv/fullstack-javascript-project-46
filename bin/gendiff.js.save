#!/usr/bin/env node

import { program } from 'commander';

// const program = new Command();

program
  .description('some info');
  .option('-h, --help', 'output usage information')
  .option('-f, --format <type>', 'output format')
  .version('1.0.0');
//  .parse();

program.parse();

const options = program.opts(); 

if (options.help) {
  const textHelp = `Usage: gendiff [options]

Compares two configuration files and shows a difference.

Options:
  -V, --version        output the version number
  -h, --help           output usage information`;
  console.log(textHelp);
};

Библиотека Commander существует для разработки утилит командной строки. Она позволяет оперировать в коде аргументами и опциями, которые были переданы в командной строке
